@using IntexWinter2024.Infrastructure

@model Product

<div class="card m-1 p-1">
    <h4>@Model.Name</h4>
    <ul>
        <li>$@Model.Price</li>
        <li>@Model.PrimaryColor</li>
        <li>Year Released: @Model.Year</li>
@*         @if (Model.Categories.Count == 1)
        {
            <li>Category: @Model.Categories.First()</li>
        }
        else
        {
            <li>Categories: @string.Join(", ", Model.Categories)</li>
        } *@
    </ul>
    <form id="@Model.ProductId" method="post" asp-page="/Cart">
        <input type="hidden" asp-for="ProductId" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
        <button type="submit" class="btn btn-success">Add To Cart</button>
    </form>
</div>